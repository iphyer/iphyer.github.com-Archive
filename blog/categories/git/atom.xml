<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Git | 桑弧蓬矢射四方]]></title>
  <link href="http://iphyer.github.com/blog/categories/git/atom.xml" rel="self"/>
  <link href="http://iphyer.github.com/"/>
  <updated>2018-08-24T11:59:19+08:00</updated>
  <id>http://iphyer.github.com/</id>
  <author>
    <name><![CDATA[iphyer]]></name>
    <email><![CDATA[iphyer@163.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Git使用总结]]></title>
    <link href="http://iphyer.github.com/blog/2016/02/17/git/"/>
    <updated>2016-02-17T15:57:00+08:00</updated>
    <id>http://iphyer.github.com/blog/2016/02/17/git</id>
    <content type="html"><![CDATA[<h1 id="section">前言</h1>

<p>使用 Git 的频率越来越高，这里总结下自己经常用的命令。参考了这篇博文<a href="http://blog.csdn.net/wfdtxz/article/category/1223316">如何使用git创建项目，创建分支 </a></p>

<!--more-->

<h1 id="section-1">总结</h1>

<h2 id="section-2">初始配置</h2>

<p>```
git config -global user.name “Your name”</p>

<p>git config -global user.email “you@example.com”
```</p>

<h2 id="section-3">使用</h2>

<p><code>git init</code>命令用于初始化当前所在目录的这个项目</p>

<p><code>git status</code> 查看项目状态</p>

<p><code>git add ..</code> 给我目前的这个项目制作一个快照snapshot（快照只是登记留名，快照不等于记录在案，git管快照叫做索引index)</p>

<p><code>git commit</code> 用于将快照里登记的内容永久写入git仓库</p>

<p><code>git commit -a</code>这个命令可以直接提交所有修改，省去了你<code>git add</code>和<code>git diff</code>和<code>git commit</code>的工序</p>

<p>注意：无法把新增文件或文件夹加入进来，所以，如果你新增了文件或文件夹，那么就要老老实实的先<code>git add .</code>，再<code>git commit</code></p>

<p><code>git log -p</code> git不但会给出开发日志，而且会显示每个开发版本的代码区别所在。</p>

<blockquote>
  <p>总结：先git add你修改过的文件，再git diff并git status查看确认，然后git commit提交，然后输入你的开发日志，最后git log再次确认。</p>
</blockquote>

<h2 id="section-4">创建分支</h2>
<p><code>git branch</code> 查看分支列表</p>

<p><code>git branch experiment</code> 创建一个分支</p>

<p><code>git checkout experiment</code> 切换分支到experiment</p>

<p><code>git commit -a</code>  在分支上提交工作</p>

<p><code>git checkout master</code> 切换主干道</p>

<p><code>git merge experiment</code> 合并分支到主干道</p>

<p><code>git branch -d experiment </code> -d，表示“在分支已经合并到主干后删除分支”。如果使用大写的-D的话，则表示“不论如何都删除分支”，</p>
]]></content>
  </entry>
  
</feed>
